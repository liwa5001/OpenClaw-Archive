# 2026-03-13 - 飞书 ↔ Webchat 镜像系统配置完成

## 🪞 镜像系统配置

### 完成时间
2026-03-13 11:20-12:19

### 配置内容

**双向消息镜像系统：**
- 飞书 → Webchat：Cloudflare Tunnel + Webhook 服务器
- Webchat → 飞书：Cron 轮询（每 1 分钟）

### 关键配置

| 组件 | 配置 |
|------|------|
| Webhook 服务器 | `scripts/feishu-webhook-server-v2.js` (端口 8899) |
| 公网穿透 | Cloudflare Tunnel (`cloudflared tunnel --url http://localhost:8899`) |
| 公网 URL | `https://trip-cyber-powered-scratch.trycloudflare.com/feishu/webhook` |
| Webchat→飞书 Cron | ID: `928de2ec-1449-489c-b953-0dd03c6c556e` |
| 飞书应用 ID | `cli_a92ebaa37eb89cb0` |
| 飞书用户 ID | `ou_7781abd1e83eae23ccf01fe627f0747f` |

### 相关文件

| 文件 | 用途 |
|------|------|
| `scripts/feishu-webhook-server-v2.js` | 飞书 webhook 服务器 |
| `scripts/webchat-to-feishu-sync-v5.sh` | Webchat 轮询脚本 |
| `scripts/start-feishu-mirror-stable.sh` | 一键启动脚本（Cloudflared） |
| `config/feishu-webhook-config.json` | 配置文件 |
| `logs/feishu-webhook.log` | Webhook 日志 |
| `logs/cloudflared-feishu.log` | Cloudflared 日志 |
| `logs/webchat-to-feishu.log` | 同步日志 |

### 飞书后台配置

1. 登录 https://open.feishu.cn/app
2. 应用：Openclaw (App ID: cli_a92ebaa37eb89cb0)
3. 事件与回调 → 回调配置
4. 订阅方式：将回调发送至开发者服务器
5. 请求地址：`https://trip-cyber-powered-scratch.trycloudflare.com/feishu/webhook`
6. 订阅事件：`im.message.receive_v1`

### 管理命令

```bash
# 启动稳定版
./scripts/start-feishu-mirror-stable.sh

# 查看当前 URL
cat logs/feishu-mirror-url.txt

# 查看状态
curl http://localhost:8899/status

# 查看日志
tail -f logs/feishu-webhook.log
tail -f logs/cloudflared-feishu.log
```

### 注意事项

1. **Cloudflared URL 变化**：每次重启 cloudflared 会生成新 URL，需要更新飞书后台配置
2. **验证 Token**：当前未配置 verifyToken，生产环境建议配置
3. **消息去重**：使用 messageId + timestamp 去重，防止循环转发
4. **Cron 频率**：Webchat→飞书 每 1 分钟轮询一次

### 测试状态

- ✅ Webhook 服务器运行正常
- ✅ Cloudflare Tunnel 连接正常
- ✅ URL 验证成功（返回 challenge）
- ⏳ 飞书后台配置待完成
- ⏳ 双向消息测试待完成

---

**维护者：** 城堡 🏰  
**下次检查：** 确认飞书后台配置完成并测试双向消息同步
